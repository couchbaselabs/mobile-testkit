{
	"interface":":4984",
    "adminInterface": "0.0.0.0:4985",
	"log": ["*"],
	"databases": {
		"db": {
			"revs_limit" : 10,
			"server": "walrus:",
			"sync":`function(doc){
				channel(doc.channels);
			}`,
		}
	}
}