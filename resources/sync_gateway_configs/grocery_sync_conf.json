{
	{{ logging }}
	{{ hide_product_version }}
	{{ disable_persistent_config }}
	"databases": {
		"grocery-sync": {
			"server": "walrus:",
			"sync": `
function(doc){
	channel(doc.channels);
}`
		}

	}
}
