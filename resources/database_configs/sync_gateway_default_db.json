{
"databases":{
    "db":{
        {{ autoimport }}
        {{ xattrs }}
        {{ no_conflicts }}
        {{ sg_use_views }}
        {{ num_index_replicas }}
        {{ username }}
        {{ password }}
        {{ cacertpath }}
        {{ certpath }}
        {{ keypath }}
        {{ delta_sync }}
        {{ bucket }}
        {{ revs_limit }}
        "bucket":"data-bucket"
        "server":"{{ server_scheme }}://{{ couchbase_server_primary_node }}:{{ server_port }}"
    }
}
}